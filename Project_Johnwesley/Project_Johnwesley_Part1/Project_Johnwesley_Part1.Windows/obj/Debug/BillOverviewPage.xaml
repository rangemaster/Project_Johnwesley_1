<Page
    x:Name="pageRoot"
    x:Class="Project_Johnwesley_Part1.BillOverviewPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_Johnwesley_Part1"
    xmlns:common="using:Project_Johnwesley_Part1.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">Bill Overview</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Width="900">
                <StackPanel Orientation="Horizontal" Width="900" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Width="400">
                        <Button x:ConnectionId='7' Content="Add"                          />
                        <Button x:ConnectionId='8' Content="Remove"                             />
                        <Button x:ConnectionId='9' Content="Save"                           />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Width="400" VerticalAlignment="Center">
                        <Button x:ConnectionId='4' Content="Add"                           />
                        <Button x:ConnectionId='5' Content="Remove"                              />
                        <Button x:ConnectionId='6' Content="Save"                            />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <ScrollViewer Width="400" Height="500" >
                        <StackPanel x:Name="Overview_Year_Panel" VerticalAlignment="Center">

                        </StackPanel>
                    </ScrollViewer>
                    <ScrollViewer Width="400" Height="500" >
                        <StackPanel x:Name="Overview_Month_Panel" VerticalAlignment="Center">

                        </StackPanel>
                    </ScrollViewer>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button x:ConnectionId='3' x:Name="_Go_Bn" Content="Go!"                      FontSize="32" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="_Feedback_tx" Text="..." FontSize="12" Foreground="Aqua"/>
                </StackPanel>
            </StackPanel>
        </Grid>
        <FlyoutPresenter Grid.Row="1" x:Name="Add_Flyout" Width="200" Height="150" HorizontalAlignment="Left" Visibility="Collapsed">
            <Grid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
                    <TextBox x:Name="Flyout_Input_tx" Text="" TextWrapping="Wrap" FontSize="20" Width="150"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                    <Button x:ConnectionId='1' x:Name="Flyout_Close_bn" Content="Close"                              />
                    <Button x:ConnectionId='2' x:Name="Flyout_Save_bn" Content="Save"                             />
                </StackPanel>
            </Grid>
        </FlyoutPresenter>
    </Grid>
</Page>

